
int global1, global2 = 10;
int n, p;

int vector[3]= {1,2,4};

int main(int a, int b){

    // int result = a > b ? x : y;
    char letter;

    // int a = 5;
    // int a[10][20];
    
    vector[1] = 42;
    int vec[2] = {1,3};    
    int vect[2][2][2] = {{{1,2},{3,4}},{{5,6},{7,8}}};
    vect[2][0][0] = 12312; 
    
    // while(0) ab = 10;
    
    while(1){
     
        continue;
     
    }
    
    if (3 > 1) puts("hello world!");
    // hola();
    // hola(a, 1, 2);
    
    // switch( i ) 
    switch( 1 ) 
    {
        case -1:
            n++;
            break;
        case 0 :
            // z++;
            break;
        case 1 :
            p++;
            break;
        default:
            break;
    }
    
    // char letter;
    
    return 0;
}

int main2(char a, int b, double c){}

int f();
int g();
